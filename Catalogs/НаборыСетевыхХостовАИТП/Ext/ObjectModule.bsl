#Область ПрограммныйИнтерфейс

// Функция - Возвращает массив, содержащий имена хостов в наборе
// 
// Возвращаемое значение:
//   Массив - массив имен хостов
//
Функция ПолучитьИменаХостов() Экспорт
	
	ИменаХостов = Новый Массив();
	
	Для Каждого Компьютер Из ЭтотОбъект.СписокКомпьютеров Цикл
		ИменаХостов.Добавить(Компьютер.Компьютер.ИмяХоста);
	КонецЦикла;
	
	Возврат ИменаХостов;
	
КонецФункции

#КонецОбласти