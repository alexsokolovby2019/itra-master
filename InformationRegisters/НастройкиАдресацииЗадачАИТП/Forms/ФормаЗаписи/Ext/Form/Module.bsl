#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ТочкаМаршрутаОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	// Вставить содержимое обработчика.
	Если ТипЗнч(ВыбранноеЗначение) = Тип("Тип") Тогда
		Запись.БизнесПроцесс = СтрЗаменить(Строка(ВыбранноеЗначение), "(точка маршрута)", "");
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти